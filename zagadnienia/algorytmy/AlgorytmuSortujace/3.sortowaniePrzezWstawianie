def sortowaniePrzezWstawianie(A):
    
    n = len(A)                                      # Długość tablicy
    i = 1                                           # Iterator który wybiera element do sprawdzenia (element po prawej od number) 

    while i < n:
        
        temp = A[i]                                 # Element o indeksie o jeden większy od poprzedniej iteracji (element po prawej)
        j = i - 1                                   # Indexs elementu po lewej (Element do którego będziemy porównywać element z prawej strony)

        while j >= 0 and temp < A[j]:               # Dopóki nie dojdziemy do części listy oporządkowanej i nie znajdziemy elementu mniejszego od wybranego
            A[j+1] = A[j]                           # Jeśli element po lewej będzie większy od elementu po prawej to zmień go w element po prawej
            j -= 1                                  # Wykonaj jeszcze raz pętle która powtórzy czynność patrząc czy element temp jest jeszcze mniejszy niż kolejny element po lewej i tak dalej póki nie dojdziemy do początku tablicy
        A[j+1] = temp                               # Jeśli element po prawej nie jest mniejszy od elementu w grupie uporządkowanej to zostaw go na swoim miejscu i wybierz następny element po prawej
        i += 1                                      # Przejście do następnego elementu

    return A
